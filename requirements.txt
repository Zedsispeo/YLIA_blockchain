# --- Cœur de l'application ---
Flask==3.1.3
ecdsa==0.19.1          # cryptographie réelle (ECDSA / SECP256k1)
requests==2.32.3       # communication HTTP entre nœuds

# --- Dépendances transitives de Flask (épinglées) ---
blinker==1.9.0
click==8.4.1
itsdangerous==2.2.0
Jinja2==3.1.6
MarkupSafe==3.0.3
Werkzeug==3.1.8

# --- Tests (optionnel pour exécuter, requis pour `pytest`) ---
pytest==8.3.4

# CORS support for the demo dashboard
Flask-Cors==4.0.0
